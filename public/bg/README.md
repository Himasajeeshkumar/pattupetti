Drop your two background images in here:

- scene-wide.png — landscape, used by default and on wide/landscape viewports
- scene-tall.png — portrait, used when `(orientation: portrait)` matches;
  compose it separately, don't just crop the wide one

Referenced from `app/globals.css` via `.hero-bg`.
